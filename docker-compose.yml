services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: solicitacao_egpce_app
    working_dir: /var/www
    user: "www-data"
    volumes:
      - ./:/var/www
    depends_on:
      - db
      - redis
    networks:
      - solicitacao_egpce

  nginx:
    image: nginx:alpine
    container_name: solicitacao_egpce_nginx
    ports:
      - "8036:80"
    volumes:
      - ./:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - solicitacao_egpce

  db:
    image: postgres:16-alpine
    container_name: solicitacao_egpce_db
    environment:
      POSTGRES_DB: solicitacao_egpce
      POSTGRES_USER: root
      POSTGRES_PASSWORD: password
      TZ: America/Fortaleza
    ports:
      - "8037:5432"
    volumes:
      - solicitacao_egpce_db:/var/lib/postgresql/data
    networks:
      - solicitacao_egpce

  redis:
    image: redis:alpine
    container_name: solicitacao_egpce_redis
    ports:
      - "8038:6379"
    networks:
      - solicitacao_egpce

  node:
    image: node:20-alpine
    container_name: solicitacao_egpce_node
    working_dir: /var/www
    volumes:
      - ./:/var/www
    ports:
      - "8039:5173"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    networks:
      - solicitacao_egpce
    depends_on:
      - app

networks:
  solicitacao_egpce:

volumes:
  solicitacao_egpce_db:
